// Placeholders removed: every story now has its own AI-generated cover stored in `stories.cover_image`.
// Keeping this helper as a no-op for backwards compatibility with any imports.
export const getStoryImage = (_title: string): string | null => null;